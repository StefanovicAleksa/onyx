[pytest]
# 1. Add the current directory to PYTHONPATH so we can import 'app'
pythonpath = .

# 2. Default flags: 
# -v: Verbose output (shows every test name)
# -s: Show print statements (crucial for seeing our file paths)
addopts = -v -s

# 3. Where to look for tests
testpaths = tests